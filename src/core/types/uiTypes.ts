export type TypeIcon = {
  path: string;
  viewBox: string;
};

export type Tcard = {
  path: string;
  viewBox: string;
  price: string;
  name: string;
};

export enum Theme {
  Black = "black",
  White = "white",
}
